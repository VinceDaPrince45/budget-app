<h1><strong>Store: <%= store.name %></strong></h1>

<% if (store_expenses.length > 0) { %>
  <p><strong>Delete the following expenses before attempting to delete this store.</strong></p>
  <div style="margin-left:20px;margin-top:20px">
    <h4>Expenses:</h4>
    <dl>
      <% store_expenses.forEach(function(expense) { %>
        <dt>
            <a href="<%= expense.url %>"><%= expense.item_name %></a>
            <p>$<%= expense.price %></p>
        </dt>
      <% }); %>
    </dl>
  </div>
<% } else { %>
  <p>Do you really want to delete this store?</p>

  <form method="POST">
    <div class="form-group">
      <input id="store_id" class="form-control" type="hidden" name="store_id" value="<%= store._id %>">
    </div>
    <button class="btn btn-primary" type="submit">Delete</button>
  </form>
<% } %>